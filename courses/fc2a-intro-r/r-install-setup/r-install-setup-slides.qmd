---
title: "R Installation and Setup"
subtitle: "Companion slides"
format:
  revealjs:
    slide-number: true
    progress: true
    hash: true
    center: false
    navigation-mode: linear
    transition: fade
    background-transition: fade
    menu: true
    footer: "mds-tools ? R Installation and Setup"
---
<!-- AUTO-GENERATED SLIDES: edit freely; regenerate with care -->

## Overview

- This unit covers the installation and setup of R.

::: {.notes}
In this slide, we focus on overview and why it matters. The key ideas here are This unit covers the installation and setup of R. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Goals

- Download and install the appropriate R version for your platform.
- Get R running on your machine.

::: {.notes}
In this slide, we focus on goals and why it matters. The key ideas here are Download and install the appropriate R version for your platform., Get R running on your machine. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Installation

- R can be freely downloaded from [its website](https://www.r-project.org/).
- Pick the version that matches your operating system and architecture.
- It should be possible for you to install it without
- However, your mileage might vary and it is possible that
- You might be asked some questions during the install process.
- It is safe to always go with the defaults, unless

::: {.notes}
In this slide, we focus on installation and why it matters. The key ideas here are R can be freely downloaded from [its website](https://www.r-project.org/)., Pick the version that matches your operating system and architecture., It should be possible for you to install it without. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Setup

- Once R is installed, you can open it.
- The graphical interface you see will look pretty outdated.
- That's ok, we basically never use R through its own

::: {.notes}
In this slide, we focus on setup and why it matters. The key ideas here are Once R is installed, you can open it., The graphical interface you see will look pretty outdated., That's ok, we basically never use R through its own. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Quick spin

- Just to make sure `R` is running, you can type
- E.g.
- if you type `2+2`, you should get `4` as a
- Most of the power of R comes from add-ons called
- For now, you can try to install one.
- Type `install.packages('dplyr')` into the console.

::: {.notes}
In this slide, we focus on quick spin and why it matters. The key ideas here are Just to make sure `R` is running, you can type, E.g., if you type `2+2`, you should get `4` as a. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Updating R

- R is pretty stable and not much changes between versions.
- As long as you have one of the more recent
- If you want to upgrade, just install the new version.
- It will not override the old one, so if you
- Note that when you update to a new version of

::: {.notes}
In this slide, we focus on updating r and why it matters. The key ideas here are R is pretty stable and not much changes between versions., As long as you have one of the more recent, If you want to upgrade, just install the new version. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Rtools Install

- If you just plan to use R and R packages
- However, if you plan on compiling packages from source on
- Make sure to download the version that matches your R
- Follow the installation instructions on that page.

::: {.notes}
In this slide, we focus on rtools install and why it matters. The key ideas here are If you just plan to use R and R packages, However, if you plan on compiling packages from source on, Make sure to download the version that matches your R. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Summary

- We discussed how to install R and how to keep
- You should now be ready to use it.
- You should now be ready to use it.

::: {.notes}
In this slide, we focus on summary and why it matters. The key ideas here are We discussed how to install R and how to keep, You should now be ready to use it., You should now be ready to use it. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::
