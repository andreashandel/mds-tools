---
title: "Quarto and Positron"
subtitle: "Companion slides"
format:
  revealjs:
    slide-number: true
    progress: true
    hash: true
    center: false
    navigation-mode: linear
    transition: fade
    background-transition: fade
    menu: true
    footer: "mds-tools ? Quarto and Positron"
---
<!-- AUTO-GENERATED SLIDES: edit freely; regenerate with care -->

## Overview

- This unit explains how to use Quarto within the Positron

::: {.notes}
In this slide, we focus on overview and why it matters. The key ideas here are This unit explains how to use Quarto within the Positron. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Goals

- Be able to create and render basic Quarto documents through Positron.
- Be able to create and render basic Quarto documents through Positron.

::: {.notes}
In this slide, we focus on goals and why it matters. The key ideas here are Be able to create and render basic Quarto documents through Positron., Be able to create and render basic Quarto documents through Positron. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Quarto and Positron

- Quarto documents are basically text documents.
- They have the file extension `.qmd` (Quarto Markdown).
- You can create and edit these files in any text

::: {.notes}
In this slide, we focus on quarto and positron and why it matters. The key ideas here are Quarto documents are basically text documents., They have the file extension `.qmd` (Quarto Markdown)., You can create and edit these files in any text. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Creating Quarto documents with Positron

- Start Positron.
- Then start a project (or open an existing one) as
- Next, create a new file by going to `File` ->
- A new tab opens with a basic Quarto document template.
- We'll talk more about what Quarto documents look like in
- For now, let's just create a simple Quarto document to

::: {.notes}
In this slide, we focus on creating quarto documents with positron and why it matters. The key ideas here are Start Positron., Then start a project (or open an existing one) as, Next, create a new file by going to `File` ->. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## This is a subheading

- Here is text.
- **This is bold text.** Formatting follows the Markdown syntax.
- **This is bold text.** Formatting follows the Markdown syntax.

::: {.notes}
In this slide, we focus on this is a subheading and why it matters. The key ideas here are Here is text., **This is bold text.** Formatting follows the Markdown syntax., **This is bold text.** Formatting follows the Markdown syntax. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## A code example

- Here is a simple example demonstrating the integration of R
- The code is executed when the Quarto document is processed
- The code is executed when the Quarto document is processed

```text
x <- seq(1, 10)
y <- x^2
plot(x,y)
```

::: {.notes}
In this slide, we focus on a code example and why it matters. The key ideas here are Here is a simple example demonstrating the integration of R, The code is executed when the Quarto document is processed, The code is executed when the Quarto document is processed. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Rendering

- Once you have written your document and want to see
- Rendering is the name used by Quarto for turning your
- The easiest way to do this is to click the
- Positron will execute the `quarto preview` command in the terminal
- This both renders the document and opens the resulting file
- a web browser for HTML output or MS Word for

```text
quarto render name-of-file.qmd
```

::: {.notes}
In this slide, we focus on rendering and why it matters. The key ideas here are Once you have written your document and want to see, Rendering is the name used by Quarto for turning your, The easiest way to do this is to click the. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Customizing Quarto Settings in Positron

- Positron comes with the Quarto extension pre-installed, providing seamless integration
- You can change settings for Quarto as you learned previously
- You can change settings for Quarto as you learned previously

::: {.notes}
In this slide, we focus on customizing quarto settings in positron and why it matters. The key ideas here are Positron comes with the Quarto extension pre-installed, providing seamless integration, You can change settings for Quarto as you learned previously, You can change settings for Quarto as you learned previously. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Debugging and Troubleshooting

- If you encounter issues when using Quarto, you will usually
- Quite often, rendering fails if links are broken, code doesn't
- Quite often, rendering fails if links are broken, code doesn't

::: {.notes}
In this slide, we focus on debugging and troubleshooting and why it matters. The key ideas here are If you encounter issues when using Quarto, you will usually, Quite often, rendering fails if links are broken, code doesn't, Quite often, rendering fails if links are broken, code doesn't. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::

## Summary

- Quarto is nicely integrated into Positron, allowing you to create,
- Quarto is nicely integrated into Positron, allowing you to create,
- Quarto is nicely integrated into Positron, allowing you to create,

::: {.notes}
In this slide, we focus on summary and why it matters. The key ideas here are Quarto is nicely integrated into Positron, allowing you to create,, Quarto is nicely integrated into Positron, allowing you to create,, Quarto is nicely integrated into Positron, allowing you to create,. As you study this, think about how these points connect to your own work or projects. If something feels abstract, pause and re-read the unit text for concrete examples, then come back to the slide. The goal is not to memorize every detail, but to build a clear mental map you can use later.
:::
